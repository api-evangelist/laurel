slug: laurel
provider: Laurel
generated_by: planning/capability-mapping/scripts/classify_capabilities.py
model: claude-opus-5
frame:
- Healthcare Providers
min_confidence: 0.7
capability_model:
  source: https://github.com/vincentmakes/turbo-ea-capabilities
  license: CC-BY-4.0
  attribution: Turbo EA Capabilities by Vincent Verdet — Turbo EA, https://github.com/vincentmakes/turbo-ea-capabilities,
    CC BY 4.0
  notice: NOTICE
edge_count: 2
edges:
- tag: Customers
  spec_file: laurel-customers-api-openapi.yml
  capability_id: BC-4230
  capability_id_l1: BC-4230
  capability_name: SaaS Tenant Management
  confidence: 0.75
  evidence: POST /api/v1/customers; GET /api/v1/customers/{customerId}/tenant; 'Enable or disable SCIM
    provisioning on a connection'; 'Update the domains (FQDNs) for a connection, propagating to Auth0
    HRD'
  reason: Operations create and configure tenants ('customers') including tenant identity connections,
    SCIM provisioning and region — multi-tenant SaaS tenant provisioning and identity federation. Spans
    both tenant lifecycle and identity federation sub-capabilities, so L1 only.
- tag: Users
  spec_file: laurel-users-api-openapi.yml
  capability_id: BC-620.20
  capability_id_l1: BC-620
  capability_name: Identity & Access Management
  confidence: 0.7
  evidence: POST /api/v1/customers/{customerId}/users/{userId}/identities CustomerUserController_createNewUserIdentity_v1;
    schemas RoleDigest, PermissionDigest, OnAuthRequestDto, UpdateUserStatusDto
  reason: User records, identity aliases, roles/permissions, delegates and auth hooks constitute identity
    and access administration for the platform, not employee HR records. Mapped to IAM rather than HR
    Operations because the schemas centre on identities, roles and permissions.
